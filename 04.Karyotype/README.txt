This folder is used to ancestral karyotype analysis.

(1) a.DESCHRAMBLER.sh the command is used to identify the synteny fragment in all sepcies. The contral file in example/DESCHRAMBLER

(2) b.anges.sh :constructed the karyotype for each nodes. The contral file in example/ANGES

(3) c.merge_karyotype.sh: karyotype plot

(4) d.grimm.sh: the varaint of karyotype between two nodes.

